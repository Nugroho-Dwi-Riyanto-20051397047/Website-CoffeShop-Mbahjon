@font-face {
    font-family: Sunday;
    src: url(../font/Sunday.ttf);
    font-style: normal;
}

@font-face {
    font-family: Santana;
    src: url(../font/SantanaXtraCondensed.ttf);
    font-style: normal;

}

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: Sunday;
}

section {
    position: relative;
    width: 100%;
    min-height: 100vh;
    padding: 100px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    background: white;
}

header {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    padding: 20px 200px;
    display: flex;
    justify-content: space-between;
    align-items: center;
}

header .logo {
    position: relative;
    max-width: 80px;
    margin-left: -130px;
}

header ul {
    position: relative;
    display: flex;
}

header ul li {
    list-style: none;
}

header ul li a {
    display: inline-block;
    color: black;
    font-weight: 800;
    margin-left: 60px;
    text-decoration: none;
}

header .sign {
    position: absolute;
    margin-left: 50px;
    min-width: 70px;
    min-height: 25px;
    font-family: Cambria, Cochin, Georgia, Times, 'Times New Roman', serif;
    border-radius: 5px;
    background: green;
    color: white;
    font-weight: 800;
    border: none;
    cursor: pointer;
}

.content {
    position: relative;
    width: 100%;
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.content .textBox {
    position: absolute;
    width: 70%;
}

.content .textBox h2 {
    color: black;
    font-family: Sunday;
    font-size: 2.5em;
    line-height: 1.2em;
    font-weight: 900;
    letter-spacing: 5px;
}

.content .textBox span {
    color: #3c623d;
    font-family: Sunday;
    font-size: 1em;
    font-weight: 900;
}

.content .imgBox {
    position: absolute;
    margin-left: 40%;
}

.content .imgBox img {
    width: 800px;
    height: 800px;
    margin-top: 35%;
    margin-left: 15%;
}

.kopi {
    position: absolute;
    left: 40%;
    top: 420px;
    transform: translateX(-50%);
    display: flex;
}

.kopi li {
    list-style: none;
    display: inline-block;
    margin: 0 -40px;
    cursor: pointer;
    transition: 0.5s;
}

.kopi li:hover {
    transform: translateY(-15px);
}

.kopi li img {
    max-width: 200px;
}

.circle {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: #3c623d;
    clip-path: circle(600px at right 800px);
}
